{
  "buildCommand": "cd ../.. && pnpm run build:shared && cd apps/web && pnpm build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "rootDirectory": "apps/web"
}
